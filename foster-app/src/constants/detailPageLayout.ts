/**
 * Margin below the detail header block (title row + badges) before the next section
 * (coordinator actions like Assign Foster, foster CTAs like Request to Foster, or body content).
 *
 * Use this on animal/group (and similar) detail pages so coordinator and foster views match.
 */
// Slightly more breathing room than mb-2 to prevent the CTA row feeling cramped.
export const DETAIL_HEADER_BOTTOM = "mb-3";
